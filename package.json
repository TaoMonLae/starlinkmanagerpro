{
  "name": "starlink-manager-pro",
  "version": "5.0.0",
  "private": true,
  "description": "Private SaaS dashboard for managing Starlink accounts, billing and renewals.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "predev": "node scripts/check-workspace-clean.js",
    "dev": "concurrently \"npm run dev -w server\" \"npm run dev -w client\"",
    "install:all": "npm install",
    "build": "npm run build -w client",
    "start": "npm run start -w server",
    "prisma:migrate": "prisma migrate dev",
    "prisma:seed": "prisma db seed",
    "validate": "npm run build -w client && npm run check -w server && npm audit --omit=dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "prisma": "^5.22.0"
  },
  "prisma": {
    "schema": "server/prisma/schema.prisma",
    "seed": "node server/prisma/seed.js"
  }
}
